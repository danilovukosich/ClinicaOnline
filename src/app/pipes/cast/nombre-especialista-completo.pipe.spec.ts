import { NombreEspecialistaCompletoPipe } from './nombre-especialista-completo.pipe';

describe('NombreEspecialistaCompletoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreEspecialistaCompletoPipe();
    expect(pipe).toBeTruthy();
  });
});
